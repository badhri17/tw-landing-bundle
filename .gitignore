# Dependencies
node_modules/

# Build output — regenerated by `pnpm build`, and what tw-preview uploads from
dist/

# tw-preview snapshot id + write key (machine-local, not shareable)
.salla-preview.json

# Editor / OS noise
.DS_Store
*.log

# Local scratch/injection script — not part of the bundle
test.js
